# npm registry configuration
# This allows semantic-release to publish with OIDC token
# The NPM_TOKEN environment variable will be set by GitHub Actions via OIDC
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
